import 'package:flutter/material.dart';

// 积分中心页面
class IntegralCentre extends StatelessWidget {
  const IntegralCentre({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("积分中心"),
      ),
    );
  }
}
